import React from 'react'

function Activities(props) {
  return (
    <div>Activities
        <p>{props.title}</p>
        <p>{props.title}</p>
        <p>{props.title}</p>
    </div>
    
  )
}

export default Activities